---
# De onde vem a informacao. As fontes correm por esta ordem.
#
# Tipos:
#   manual        registo em ficheiro, com prova (URL) em cada linha. Ha
#                 duas: o registo do canal (config/entrevistas.yml), que e
#                 a fonte principal e e escrito por uma pessoa que abriu a
#                 pagina, e o clipping de imprensa (config/clipping.yml),
#                 que desde 2026-09-09 e gerado pela ferramenta a partir
#                 das quatro condicoes, com uma pessoa a poder vetar
#                 linhas. Corre depois do registo do canal: quando os dois
#                 cobrem a mesma emissao, fica a do canal.
#   registo_automatico
#                 o mesmo ficheiro e o mesmo formato do `manual`, mas sem
#                 a isencao: o criterio trata-o como fonte automatica, com
#                 prova positiva de formato e rondas de confirmacao. E o
#                 que permite publicar provas de canal que a ferramenta
#                 colheu sozinha, sem afirmar que alguem as leu.
#   podcast_rss   feed RSS de podcast: da titulo e data. Entra sozinho se
#                 o criterio deixar. Usar `segmentos` quando o feed mistura
#                 programas; `formato` diferente de "entrevista" exclui.
#   youtube_feed  feed Atom publico de um canal de YouTube, sem conta nem
#                 chave. Passa pelo criterio como qualquer fonte
#                 automatica: prova de formato no titulo e rondas.
#
# Nao ha duracao em fonte nenhuma desde 2026-09-10: o site conta a
# existencia de cada entrevista, nao o tempo. O campo `duracao_opcional`
# deixou de existir.
#
# Antes de ativar uma fonte nova: --dry-run, ler a quarentena, contar os
# motivos, ler dez titulos aceites a mao contra o criterio. Depois ativar.

fontes:
  # Curadoria a mao: o autor acrescenta uma emissao com o dia, o canal e a
  # prova, e nada mais. Corre a frente de tudo porque e a decisao mais
  # informada que existe no projeto, e quando cobre a mesma emissao que
  # outra fonte e a dela que fica. A isencao de prova de formato e de
  # rondas e a mesma do registo curado e tem a mesma justificacao: uma
  # pessoa abriu a pagina. A `origem` aqui e so a que vale por omissao; a
  # linha pode escrever a sua, porque a prova tanto pode ser a pagina do
  # canal como a noticia que a relata.
  #
  # O mesmo ficheiro tem a lista `remover`, que nao e uma fonte e nao e
  # lida por aqui. Ver recolha/curadoria.py.
  - id: curadoria
    tipo: manual
    ativa: true
    ficheiro: config/curadoria.yml
    origem: canal
    confianca: alta

  - id: registo-curado
    tipo: manual
    ativa: true
    ficheiro: config/entrevistas.yml
    origem: canal
    confianca: alta

  # Paginas de canal que a ferramenta colheu e verificou, sem ninguem ter
  # aberto a pagina. Corre depois do registo verificado a mao, que ganha
  # sempre, e antes do clipping, porque uma pagina do canal e melhor prova
  # do que uma peca de jornal.
  #
  # `tipo: registo_automatico` le o mesmo formato do registo curado mas
  # nao tem a isencao dele: a prova positiva de formato e as rondas de
  # confirmacao aplicam-se, porque a isencao existe para quando uma pessoa
  # leu a pagina e aqui ninguem leu.
  #
  - id: paginas-de-canal
    tipo: registo_automatico
    ativa: true
    ficheiro: config/paginas_de_canal.yml
    origem: canal
    confianca: media

  # Clipping de imprensa escrita. Prova de que a emissao existiu e de
  # quando: uma peca que diga que no dia X houve entrevista ao sujeito no
  # canal Y. Vale o mesmo que a pagina do canal (decisao do autor,
  # 2026-09-10); corre depois so para que, quando as duas provam a mesma
  # emissao, fique a do canal e a emissao conte uma vez.
  - id: clipping-imprensa
    tipo: manual
    ativa: true
    ficheiro: config/clipping.yml
    origem: imprensa
    confianca: media

  # ------------------------------------------------------------------
  # Pesquisa do proprio site. A fonte automatica principal.
  #
  # Cada fonte pede a pagina de pesquisa do site, colhe os URL de artigo
  # do mesmo dominio e le os dados estruturados (schema.org, OpenGraph)
  # de cada um. Nao ha seletores CSS: o que se le e o que os sites
  # publicam para os motores de busca, que e o que sobrevive as
  # remodelacoes.
  #
  # `busca` aceita {termo} (cada termo de deteccao do sujeito, ja
  # codificado) e {pagina} (1 ate paginas_max). Sem {pagina}, uma so.
  # `padrao_artigo` aperta o que conta como URL de artigo; vazio usa a
  # heuristica generica (dois segmentos de caminho e um slug com hifen).
  #
  # Para o historico: subir paginas_max. Cada pagina a mais sao mais
  # pedidos, por isso o valor de todos os dias e baixo de proposito, e a
  # corrida de historico corre com --paginas para o subir sem editar
  # isto.
  # ------------------------------------------------------------------
  # --- Pesquisa do proprio canal, onde ela responde ao servidor -------
  #
  # Sondado em 2026-09-08. So a RTP Play e a TVI passam. As restantes
  # ficam registadas aqui com o motivo, porque saber o que foi tentado e
  # porque falhou faz parte do metodo e evita voltar a tentar.
  #
  #   SIC e SIC Noticias   403 em todos os pedidos, tambem com cabecalho
  #                        de browser. E a defesa do alojamento a maquinas
  #                        de datacenter, nao o User-Agent.
  #   CMTV                 responde 200 e devolve sempre as mesmas 28
  #                        ligacoes, todas de navegacao, com qualquer
  #                        parametro de pagina: os resultados sao montados
  #                        por JavaScript e nao existem no HTML servido.
  #   CNN Portugal         responde 200 e zero ligacoes de artigo, pela
  #                        mesma razao. Os termos que aparecem no texto
  #                        sao o eco da caixa de pesquisa.
  #   RTP noticias         a pesquisa e Google Custom Search: tudo o que
  #                        interessa vai depois do # e nunca chega ao
  #                        servidor.
  #   NOW                  o site nao tem pesquisa.

  - id: busca-rtp1
    tipo: busca_site
    ativa: true
    canal: rtp1
    dominio: rtp.pt
    confianca: media
    busca:
      - https://www.rtp.pt/play/pesquisa?q={termo}
      - https://www.rtp.pt/play/pesquisa?q={termo}%20entrevista
    padrao_artigo: '/play/p[0-9]+/e[0-9]+/'
    max_candidatos: 300
    paginas_max: 1

  # Lista de episodios de um programa de entrevistas, e nao uma pesquisa.
  #
  # Medido a 2026-09-10, de casa e sem nada de especial no pedido: a
  # porta que monta a lista de episodios (`bg_l_ep`) responde com HTML
  # servido pelo servidor, doze episodios por pagina, e pagina a serio.
  # As paginas 2, 4, 8 e 12 trouxeram doze ligacoes cada e as paginas 20
  # e 40 vieram vazias, por isso a lista acaba entre a 12 e a 20. O
  # identificador que interessa e o do PROGRAMA, nao o da temporada: com
  # o do programa o sujeito aparece logo na primeira pagina, e com os das
  # temporadas nao aparece, porque cada temporada so mostra o que e dela.
  #
  # Porque isto importa mais do que parece: a cobertura por ano do indice
  # de noticias e de 0 candidatos em 2019, 3 em 2020, 1 em 2021, 13 em
  # 2022 e 1 em 2023, e este programa emitiu durante esses anos todos. E
  # a primeira via que entra direita nesse buraco, com a pagina do
  # proprio canal como prova.
  #
  # O `programa` esta escrito aqui de proposito, e nao deduzido do
  # titulo: um episodio deste programa e um episodio deste programa, o
  # canal titula-os de formas diferentes ("Grande Entrevista - Fulano",
  # so "Fulano"), e sem o nome do programa a prova positiva de formato
  # recusaria metade deles. O nome consta da lista de programas de
  # entrevista da configuracao editorial, que e onde essa decisao vive.
  #
  # `paginas_max` fica em 2 porque a corrida diaria so precisa dos
  # episodios recentes: 24 episodios sao cerca de meio ano. A corrida de
  # historico vai ao fundo com `--paginas 20`, que e um numero acima do
  # fim medido da lista; uma pagina vazia nao custa nada.
  - id: lista-rtp-grande-entrevista
    tipo: busca_site
    ativa: true
    canal: rtp3
    programa: Grande Entrevista
    dominio: rtp.pt
    confianca: alta
    # Uma consulta so: o endereco nao usa {termo}, e sem isto a mesma
    # pagina seria pedida uma vez por cada termo de deteccao.
    termos_busca: ['André Ventura']
    busca:
      - https://www.rtp.pt/play/bg_l_ep/?listProgram=10324&page={pagina}&type=all
    padrao_artigo: '/play/p[0-9]+/e[0-9]+/'
    max_candidatos: 300
    paginas_max: 2

  - id: busca-tvi
    tipo: busca_site
    ativa: true
    canal: tvi
    dominio: tvi.iol.pt
    confianca: media
    # Confirmado na sondagem: pagina 1 e pagina 2 devolvem ligacoes
    # diferentes, logo a paginacao e real. O site nao se queixou do
    # ritmo, por isso a pausa fica na de omissao.
    termos_busca: ['André Ventura']
    busca:
      - https://tvi.iol.pt/search?pagina={pagina}&pesquisa={termo}+entrevista
    max_candidatos: 400
    paginas_max: 12

  # --- Motor de busca externo, para os canais cuja pesquisa nao serve --
  #
  # A consulta leva `site:` do dominio do canal, e so entram ligacoes
  # desse dominio: o motor e um indice para chegar a pagina do canal, e a
  # prova de cada linha continua a ser o URL do canal, nunca o do motor.
  #
  # Sondado em 2026-09-08: de dez motores testados, o Brave foi o unico
  # que respondeu com resultados utilizaveis a partir da maquina da CI, e
  # os primeiros que devolveu eram exactamente entrevistas do sujeito na
  # RTP e na SIC Noticias. Nao exige conta nem chave. O DuckDuckGo
  # responde mas embrulha os endereços num redireccionamento proprio, por
  # isso a sondagem nao os contou; fica por esclarecer, nao por excluir.
  #
  # `termos_busca` reduz cada canal a uma consulta so: tres consultas por
  # canal seriam tres vezes mais pedidos a um servico que nos pode
  # limitar, sem tres vezes mais resultados.
  #
  # Sondagem de 2026-09-08: com quatro pedidos, offset=0 e offset=1
  # devolveram vinte ligacoes diferentes cada. Mas a primeira corrida de
  # historico a serio, com trinta pedidos, levou 429 em quase todos, mesmo
  # com oito segundos de pausa e recuos de 20, 60 e 120 segundos: tres
  # canais ficaram a zero. O motor nao aguenta o volume que precisamos a
  # partir de uma maquina de datacenter.
  #
  # Fica como suplemento, nao como fonte principal: uma pagina por canal
  # e trinta segundos de pausa, o que da seis pedidos por ronda em vez de
  # trinta. Vinte resultados por canal, escolhidos por relevancia, e
  # pouco para o historico mas util todos os dias.

  - id: motor-sic
    tipo: busca_site
    ativa: true
    canal: sic
    dominio: sic.pt
    confianca: media
    termos_busca: ['André Ventura entrevista']
    busca:
      - https://search.brave.com/search?q=site%3Asic.pt+{termo}&offset={pagina0}
    max_candidatos: 200
    paginas_max: 1
    pausa_s: 30

  - id: motor-sic-noticias
    tipo: busca_site
    ativa: true
    canal: sic-noticias
    dominio: sicnoticias.pt
    confianca: media
    termos_busca: ['André Ventura entrevista']
    busca:
      - https://search.brave.com/search?q=site%3Asicnoticias.pt+{termo}&offset={pagina0}
    max_candidatos: 200
    paginas_max: 1
    pausa_s: 30

  - id: motor-cnn-portugal
    tipo: busca_site
    ativa: true
    canal: cnn-portugal
    dominio: cnnportugal.iol.pt
    confianca: media
    termos_busca: ['André Ventura entrevista']
    busca:
      - https://search.brave.com/search?q=site%3Acnnportugal.iol.pt+{termo}&offset={pagina0}
    max_candidatos: 200
    paginas_max: 1
    pausa_s: 30

  - id: motor-cmtv
    tipo: busca_site
    ativa: true
    canal: cmtv
    dominio: cmjornal.pt
    confianca: media
    termos_busca: ['André Ventura entrevista CMTV']
    busca:
      - https://search.brave.com/search?q=site%3Acmjornal.pt+{termo}&offset={pagina0}
    padrao_artigo: '/cmtv/'
    max_candidatos: 200
    paginas_max: 1
    pausa_s: 30

  - id: motor-rtp3
    tipo: busca_site
    ativa: true
    canal: rtp3
    dominio: rtp.pt
    confianca: media
    termos_busca: ['André Ventura entrevista RTP3']
    busca:
      - https://search.brave.com/search?q=site%3Artp.pt+{termo}&offset={pagina0}
    padrao_artigo: '/noticias/'
    max_candidatos: 200
    paginas_max: 1
    pausa_s: 30

  - id: motor-now
    tipo: busca_site
    ativa: true
    canal: now
    dominio: now.pt
    confianca: media
    termos_busca: ['André Ventura entrevista']
    busca:
      - https://search.brave.com/search?q=site%3Anow.pt+{termo}&offset={pagina0}
    max_candidatos: 200
    paginas_max: 1
    pausa_s: 30

  # ------------------------------------------------------------------
  # Exemplo de feed de podcast com varios programas. Copiar e adaptar
  # depois de verificar o feed concreto com ferramentas/descobrir.py.
  # ------------------------------------------------------------------
  # - id: podcast-exemplo
  #   tipo: podcast_rss
  #   ativa: false
  #   url: https://exemplo.pt/feed.rss
  #   canal: sic-noticias
  #   programa: Nome do Programa
  #   formato: entrevista
  #   confianca: alta
  #   segmentos:
  #     - se_titulo_tem: ['Nome do Debate']
  #       programa: Nome do Debate
  #       formato: debate
  #     - se_titulo_tem: []          # catch-all
  #       programa: Nome do Programa
  #       formato: entrevista
